饿汉式单例  在类初始化时就完成相关单例对象的创建，可以通过静态代码块或静态内部类的方式来进行实现
	优点:
		在于其不存在线程安全问题，对象的唯一性由虚拟机在类初始化创建时保证；
	缺点:
		在于如果对象的创建比较消耗资源，并且单例对象不一定会被使用时就会造成资源的浪费
懒汉式单例  在于在需要使用单例对象时才进行创建，如果对象存在则直接返回，如果对象不存在则创建后返回
	双重检查锁的懒汉式单例是线程安全的又保证了性能

单例模式的破坏方式
	序列化和反射攻击是两种常见的破坏单例的方式

枚举式单例 能够在保证线程安全，还能够防止序列化和反射功能
